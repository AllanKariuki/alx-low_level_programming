#include "main.h"

/**
 * _abs - return absolute value
 * @int: the integer being checked
 * Return: int
 */

int _abs(int)
{
	int abs;

	if (int < 0)
	{
		abs = int * -1;
	}else
	{
		abs = int;
	}
	return (abs);
}
