#include <stdio.h>

/**
 * main - prints single digit numbers
 * description: prints numbers from 0
 * Return: 0
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num ++)
	{
		printf("%d", num);
	}
	return (0);
}
